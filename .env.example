# 门诊失物招领系统 - 环境变量示例
#
# 使用方法：
#   1. 复制本文件为 .env （.env 不会被提交到 GitHub，见 .gitignore）
#   2. 把下面的值改成你自己的真实值
#   3. 云端部署时，在服务器上配置同样的环境变量
#
# 提醒：不要把真实的 .env 提交到 GitHub！

# 管理员/导医进入系统的口令（请改成强口令）
LF_PASSWORD=换成你自己的口令

# Flask session 加密密钥（请改成一长串随机字符，可用：python3 -c "import secrets;print(secrets.token_hex(32))"）
LF_SECRET_KEY=换成你自己的随机密钥

# 服务端口（一般不用改）
PORT=8000
