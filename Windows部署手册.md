# Windows 局域网部署手册

把系统部署到一台 Windows 电脑上当服务器，手机和其他电脑通过局域网访问。

---

## 一、准备服务器电脑（只做一次）

### 1. 装 Python

1. 打开 https://www.python.org/downloads/ 下载 Python 3.10 或 3.11（点黄色大按钮）
2. 双击安装包，**关键一步**：在安装界面底部勾选 **"Add Python to PATH"**（或 "Add python.exe to PATH"），再点 "Install Now"
3. 装完后，按 `Win + R`，输入 `cmd` 回车，在黑窗口输入：
   ```
   python --version
   ```
   能看到版本号（如 `Python 3.11.x`）就成功了

### 2. 拷程序文件夹

把整个 `待领处1` 文件夹拷到这台 Windows 电脑上（U盘或微信传都行）。
建议放在 **D 盘根目录**，例如 `D:\待领处1`（路径里尽量别有空格和中文，但中文也行）。

### 3. 双击启动

找到文件夹里的 **`start.bat`**，**双击**它。
- 首次启动会自动准备环境（约1-2分钟，需要联网装依赖）
- 看到下面的字、并自动打开浏览器，说明成功：
  ```
  ========================================
    门诊失物招领登记系统 已启动
  ========================================
  ```

> ⚠️ 这个黑窗口**千万别关**，关了服务就停了。

### 4. 设置电脑永不睡眠

服务器电脑睡眠或关机，所有人都会断线。设置一下：
- `设置` → `系统` → `电源和睡眠`
- 把"睡眠"改成 **"从不"**
- 把"屏幕"也改成从不关闭（可选，建议关显示器省电即可）

---

## 二、查看这台电脑的内网 IP

手机和其他电脑要用这个 IP 访问。

1. 按 `Win + R`，输入 `cmd` 回车
2. 输入 `ipconfig` 回车
3. 找到 **"无线局域网适配器 WLAN"**（用WiFi）或 **"以太网适配器"**（插网线）下面
4. 找到 **IPv4 地址**，形如 `192.168.1.105` 或 `192.168.31.xxx`

**记住这个 IP**，这就是访问地址，比如 `http://192.168.1.105:8000`

---

## 三、固定内网 IP（重要，防止IP变动）

路由器重启后，电脑的 IP 可能变化，导致手机存的网址失效。建议固定它：

### 方法：在路由器里绑定（推荐）

1. 浏览器登录路由器后台（一般是 `http://192.168.1.1` 或 `192.168.0.1`）
2. 找到 **"DHCP 客户端列表"** 或 **"已连接设备"**
3. 找到这台 Windows 电脑，记下它的 **MAC 地址**
4. 找到 **"IP与MAC地址绑定"** 或 **"静态地址分配"**
5. 给这台电脑绑定一个固定 IP（就用刚才查到的那个）

不同路由器界面不同，具体位置可在网上搜 "你的路由器品牌 + IP绑定"。

---

## 四、手机 / 其他电脑怎么访问

### 手机访问

1. 手机连上**和服务器同一个 WiFi**（注意：用4G/5G流量是连不上的！）
2. 打开手机浏览器（Safari / Chrome / 自带浏览器都行）
3. 地址栏输入：`http://服务器IP:8000`（例如 `http://192.168.1.105:8000`）
4. 注意是 **http** 不是 https

**加到手机桌面，下次一键打开：**
- iPhone（Safari）：点底部中间的分享图标 → "添加到主屏幕"
- 安卓（Chrome）：点右上角三个点 → "添加到主屏幕"

### 其他电脑访问

浏览器地址栏输入同样的地址 `http://服务器IP:8000` 即可。

---

## 五、Windows 防火墙（如果手机连不上）

如果电脑能打开，但手机打不开，可能是 Windows 防火墙拦了 8000 端口。

### 快速放行方法

1. 按 `Win + R`，输入 `control` 回车，打开控制面板
2. `系统和安全` → `Windows Defender 防火墙` → `高级设置`
3. 左边点 **"入站规则"** → 右边点 **"新建规则"**
4. 选 **"端口"** → 下一步 → 选 **TCP**，特定端口填 **8000** → 下一步
5. 选 **"允许连接"** → 一路下一步，名字填 "失物招领" → 完成

> 也可以更简单（但安全性低）：临时关闭防火墙测试。确认能通后再用上面方法精确放行。

---

## 六、日常使用流程

### 公众 vs 管理员（重要）

| 角色 | 怎么进 | 能做什么 |
|------|--------|----------|
| **患者/失主** | 直接打开网址 `http://服务器IP:8000` | 浏览失物、提交报失（不用登录）|
| **管理员/导医** | 打开网址后，点**右上角"管理员入口 →"**，输口令 | 登记、认领、报失处理、统计导出 |

> 口令默认 `8888`（可在服务器的环境变量 `LF_PASSWORD` 中修改）。

### 每天开机后

1. 打开服务器电脑
2. 双击 `start.bat`
3. 黑窗口出现"已启动"后，就可以去用了（不用管它）

### 每天关机前

1. 黑窗口直接点 × 关闭（服务停止）
2. 正常关机即可

### 数据备份（定期做！）

所有数据在 `待领处1` 文件夹里：
- `lostfound.db` —— 所有登记记录（最重要）
- `uploads/` —— 所有照片

**每周或每月**，把这两个东西拷一份到U盘或其他位置即可备份。

---

## 七、常见问题

**Q：手机打开显示"无法访问"？**
A：① 确认手机和电脑连的是**同一个 WiFi**；② 确认电脑的 `start.bat` 黑窗口还开着；③ 检查 IP 地址对不对；④ 检查 Windows 防火墙（见第五步）。

**Q：电脑重启后手机网址失效了？**
A：电脑的内网 IP 变了。需要固定 IP（见第三步），或重新查 IP（见第二步）。

**Q：手机能打开但拍照按钮没反应？**
A：手机浏览器在 http 下可能调不动摄像头。请点登记页的 **"📁 上传图片"** 按钮，它会直接调起手机相机/相册，一样能拍。

**Q：怎么改口令、类别、导医名单？**
A：用记事本打开 `config.py` 改里面的内容，改完**重启** `start.bat` 生效。

**Q：换电脑怎么办？**
A：把整个 `待领处1` 文件夹拷到新电脑，装好 Python，双击 `start.bat` 即可，数据都在文件夹里。

**Q：想让导医用起来更方便？**
A：每个导医在手机上把网址"添加到主屏幕"，下次像点 App 一样打开。
